# 实验三：基于 BLIP 的图像描述生成（Image Captioning）— 完整实验流程

## 一、实验概述

**实验目标**：使用 BLIP（Bootstrapping Language-Image Pre-training）模型实现图像描述生成，并完成英文→中文翻译。在此基础上进行五项优化扩展。

**核心模型**：
| 模型 | 用途 | 大小 |
|------|------|------|
| `Salesforce/blip-image-captioning-base` | 图像→英文描述 | ~900MB |
| `deepseek-ai/DeepSeek-R1-Distill-Qwen-1.5B`（优化） | 英文→中文精翻 | ~3GB |
| `yolov8n.pt` / `yolo11n.pt`（优化） | 目标检测 | ~6MB |

**产出物**：
- `caption.py` — 单图推理 + 可视化
- `batch_caption.py` — 批量推理
- `caption_beam.py` — Beam Search 多候选生成
- `caption_translate_llm.py` — 本地大模型精翻
- `caption_yolo_region.py` — YOLO 检测 + 区域描述
- `caption_retrieval.py` — 图文匹配检索
- `caption_batch_gpu.py` — GPU 批量加速推理
- `results/` — 所有输出结果

---

## 二、目录结构

在项目根目录下创建如下结构：

```
实验三/
├── images/                  # 输入图片目录
│   ├── street.jpg
│   ├── park.jpg
│   ├── room.jpg
│   └── ...                  # 任意 jpg/png/bmp 图片
├── results/                 # 输出结果目录（脚本自动创建）
├── caption.py               # ① 基础单图推理
├── batch_caption.py         # ② 基础批量推理
├── caption_beam.py          # ③ Beam Search 优化
├── caption_translate_llm.py # ④ 本地大模型精翻
├── caption_yolo_region.py   # ⑤ YOLO + BLIP 区域描述
├── caption_retrieval.py     # ⑥ 图文匹配检索
├── caption_batch_gpu.py     # ⑦ GPU 批量加速
└── requirements.txt         # 依赖清单
```

---

## 三、环境准备（已假设基础环境就绪）

### 3.1 安装依赖

```bash
pip install transformers>=4.40.0 torch>=2.0.0 pillow matplotlib sentencepiece sacremoses
pip install ultralytics>=8.0.0 accelerate
```

若使用 CUDA GPU，确认 PyTorch CUDA 版本匹配：

```bash
python -c "import torch; print(f'CUDA available: {torch.cuda.is_available()}')"
```

### 3.2 准备图片

将测试图片放入 `images/` 目录。支持的格式：`.jpg`, `.jpeg`, `.png`, `.bmp`。

---

## 四、基础功能（实验报告要求）

### 4.1 单图推理：`caption.py`

**功能**：加载 BLIP 模型 → 读取单张图片 → 生成英文描述（含无条件 + 带 prompt 两种模式）→ 翻译中文 → 可视化保存。

```python
"""
单图图像描述 + 中英翻译 + 可视化
用法: python caption.py
"""
import torch
from PIL import Image
from transformers import BlipProcessor, BlipForConditionalGeneration
from transformers import MarianMTModel, MarianTokenizer
import matplotlib
matplotlib.rcParams["font.sans-serif"] = ["SimHei"]
matplotlib.rcParams["axes.unicode_minus"] = False
import matplotlib.pyplot as plt
import os

# ── 1. 加载模型 ────────────────────────────────────────────
print("加载 BLIP 模型...")
processor = BlipProcessor.from_pretrained("Salesforce/blip-image-captioning-base")
model = BlipForConditionalGeneration.from_pretrained("Salesforce/blip-image-captioning-base")
model.eval()
print("BLIP 加载完成")

# ── 2. 加载翻译模型 ────────────────────────────────────────
print("加载翻译模型...")
trans_name = "Helsinki-NLP/opus-mt-en-zh"
trans_tokenizer = MarianTokenizer.from_pretrained(trans_name)
trans_model = MarianMTModel.from_pretrained(trans_name)
print("翻译模型加载完成")

# ── 3. 读取图片 ────────────────────────────────────────────
img_path = "images/street.jpg"  # 修改为实际路径
image = Image.open(img_path).convert("RGB")

# ── 4. 无条件生成 ──────────────────────────────────────────
inputs = processor(image, return_tensors="pt")
with torch.no_grad():
    output = model.generate(**inputs, max_new_tokens=50)
caption_en = processor.decode(output[0], skip_special_tokens=True)
print(f"无条件生成: {caption_en}")

# ── 5. 有条件生成（prompt 引导）────────────────────────────
prompt = "a photo of"
inputs_cond = processor(image, prompt, return_tensors="pt")
with torch.no_grad():
    output_cond = model.generate(**inputs_cond, max_new_tokens=50)
caption_cond = processor.decode(output_cond[0], skip_special_tokens=True)
print(f"条件生成: {caption_cond}")

# ── 6. 翻译中文 ────────────────────────────────────────────
inputs_zh = trans_tokenizer(caption_en, return_tensors="pt", padding=True)
with torch.no_grad():
    translated = trans_model.generate(**inputs_zh)
caption_zh = trans_tokenizer.decode(translated[0], skip_special_tokens=True)
print(f"中文翻译: {caption_zh}")

# ── 7. 可视化保存 ──────────────────────────────────────────
os.makedirs("results", exist_ok=True)
fig, ax = plt.subplots(figsize=(7, 5))
ax.imshow(image)
ax.axis("off")
ax.set_title(f"EN: {caption_en}\nZH: {caption_zh}", fontsize=11, pad=10)
plt.tight_layout()
plt.savefig("results/caption_result.jpg", dpi=150, bbox_inches="tight")
plt.show()
print("结果已保存到 results/caption_result.jpg")
```

### 4.2 批量推理：`batch_caption.py`

**功能**：遍历 `images/` 目录 → 批量生成英文描述 + 中文翻译 → 结果写入 `results/results.txt`。

```python
"""
批量图像描述 + 翻译
用法: python batch_caption.py
"""
import torch, os
from PIL import Image
from transformers import BlipProcessor, BlipForConditionalGeneration
from transformers import MarianMTModel, MarianTokenizer

# ── 1. 加载模型（只加载一次）────────────────────────────────
print("加载模型中...")
processor = BlipProcessor.from_pretrained("Salesforce/blip-image-captioning-base")
blip = BlipForConditionalGeneration.from_pretrained("Salesforce/blip-image-captioning-base")
blip.eval()

tokenizer = MarianTokenizer.from_pretrained("Helsinki-NLP/opus-mt-en-zh")
translator = MarianMTModel.from_pretrained("Helsinki-NLP/opus-mt-en-zh")
print("模型加载完成\n")

# ── 2. 定义工具函数 ────────────────────────────────────────
def generate_caption(image):
    inputs = processor(image, return_tensors="pt")
    with torch.no_grad():
        output = blip.generate(**inputs, max_new_tokens=50)
    return processor.decode(output[0], skip_special_tokens=True)

def translate(text):
    inputs = tokenizer(text, return_tensors="pt", padding=True)
    with torch.no_grad():
        output = translator.generate(**inputs)
    return tokenizer.decode(output[0], skip_special_tokens=True)

# ── 3. 批量处理 ────────────────────────────────────────────
img_dir = "images"
results = []
exts = {".jpg", ".jpeg", ".png", ".bmp"}
img_files = [f for f in os.listdir(img_dir)
             if os.path.splitext(f)[1].lower() in exts]

print(f"共 {len(img_files)} 张图片，开始处理...\n")

for fname in img_files:
    img_path = os.path.join(img_dir, fname)
    image = Image.open(img_path).convert("RGB")

    caption_en = generate_caption(image)
    caption_zh = translate(caption_en)

    print(f"[{fname}]")
    print(f"  EN: {caption_en}")
    print(f"  ZH: {caption_zh}\n")
    results.append((fname, caption_en, caption_zh))

# ── 4. 保存结果 ────────────────────────────────────────────
os.makedirs("results", exist_ok=True)
with open("results/results.txt", "w", encoding="utf-8") as f:
    for fname, en, zh in results:
        f.write(f"图片: {fname}\n")
        f.write(f"EN: {en}\n")
        f.write(f"ZH: {zh}\n")
        f.write("-" * 40 + "\n")

print(f"处理完成，结果保存到 results/results.txt")
```

---

## 五、优化扩展

### 5.1 Beam Search 多候选生成：`caption_beam.py`

**优化目标**：将 greedy decoding 改为 beam search，生成多个候选描述，按 logit 分数排序选出最优结果。

**核心改动**：
- `num_beams=5` — 同时探索 5 条路径
- `num_return_sequences=3` — 返回 top-3 个候选
- `num_beam_groups` 可选开启 diverse beam search

```python
"""
Beam Search 多候选生成 + 自动择优
用法: python caption_beam.py
"""
import torch
from PIL import Image
from transformers import BlipProcessor, BlipForConditionalGeneration
from transformers import MarianMTModel, MarianTokenizer
import os

processor = BlipProcessor.from_pretrained("Salesforce/blip-image-captioning-base")
model = BlipForConditionalGeneration.from_pretrained("Salesforce/blip-image-captioning-base")
model.eval()

tokenizer = MarianTokenizer.from_pretrained("Helsinki-NLP/opus-mt-en-zh")
translator = MarianMTModel.from_pretrained("Helsinki-NLP/opus-mt-en-zh")

def generate_caption_beam(image, num_beams=5, num_candidates=3):
    """
    Beam Search 生成多个候选描述
    返回: [(caption, score), ...]  按 score 降序
    """
    inputs = processor(image, return_tensors="pt")
    with torch.no_grad():
        outputs = model.generate(
            **inputs,
            max_new_tokens=50,
            num_beams=num_beams,
            num_return_sequences=num_candidates,
            output_scores=True,
            return_dict_in_generate=True,
        )
    # 计算每个序列的累积 log 概率作为置信度分数
    captions = []
    for i, seq in enumerate(outputs.sequences):
        cap = processor.decode(seq, skip_special_tokens=True)
        # 用序列长度归一化的累积分数
        score = outputs.sequences_scores[i].item() if outputs.sequences_scores is not None else 0.0
        captions.append((cap, score))
    captions.sort(key=lambda x: x[1], reverse=True)
    return captions

def translate(text):
    inputs = tokenizer(text, return_tensors="pt", padding=True)
    with torch.no_grad():
        output = translator.generate(**inputs)
    return tokenizer.decode(output[0], skip_special_tokens=True)

# ── 单图示例 ────────────────────────────────────────────────
img_path = "images/street.jpg"
image = Image.open(img_path).convert("RGB")

candidates = generate_caption_beam(image, num_beams=5, num_candidates=3)
best_en = candidates[0][0]

print(f"=== Beam Search 候选结果 ===")
for i, (cap, score) in enumerate(candidates):
    print(f"  [{i+1}] score={score:.3f} → {cap}")

print(f"\n最佳描述: {best_en}")
print(f"中文翻译: {translate(best_en)}")

# ── 批量版本 ────────────────────────────────────────────────
img_dir = "images"
exts = {".jpg", ".jpeg", ".png", ".bmp"}
img_files = [f for f in os.listdir(img_dir) if os.path.splitext(f)[1].lower() in exts]

os.makedirs("results", exist_ok=True)
with open("results/beam_results.txt", "w", encoding="utf-8") as f:
    for fname in img_files:
        image = Image.open(os.path.join(img_dir, fname)).convert("RGB")
        candidates = generate_caption_beam(image)
        best_en = candidates[0][0]
        best_zh = translate(best_en)

        f.write(f"=== {fname} ===\n")
        for i, (cap, score) in enumerate(candidates):
            f.write(f"  [{i+1}] score={score:.3f}: {cap}\n")
        f.write(f"  → 最佳中文: {best_zh}\n\n")

print("Beam Search 批量结果已保存到 results/beam_results.txt")
```

---

### 5.2 本地大模型精翻：`caption_translate_llm.py`

**优化目标**：替换 MarianMT（2020），使用 DeepSeek 做翻译，中文表达更自然。

**备选方案**（按显存需求从低到高）：

| 模型 | 显存需求 | 翻译质量 |
|------|----------|----------|
| `deepseek-ai/DeepSeek-R1-Distill-Qwen-1.5B` | ~4GB | 较好 |
| `deepseek-ai/DeepSeek-R1-Distill-Qwen-7B` | ~16GB | 优秀 |
| `deepseek-ai/DeepSeek-R1-Distill-Llama-8B` | ~18GB | 优秀 |
| 本地 Ollama/vLLM API / DeepSeek API | 由服务端决定 | 取决于模型 |

以下代码默认使用 `DeepSeek-R1-Distill-Qwen-1.5B`（平衡质量与资源）：

```python
"""
使用本地大模型（DeepSeek）进行英文→中文精翻
用法: python caption_translate_llm.py
"""
import torch, os
from PIL import Image
from transformers import (
    BlipProcessor,
    BlipForConditionalGeneration,
    AutoModelForCausalLM,
    AutoTokenizer,
)

# ── 1. 加载 BLIP ───────────────────────────────────────────
print("[1/3] 加载 BLIP...")
blip_processor = BlipProcessor.from_pretrained("Salesforce/blip-image-captioning-base")
blip = BlipForConditionalGeneration.from_pretrained("Salesforce/blip-image-captioning-base")
blip.eval()

# ── 2. 加载 DeepSeek 做翻译 ─────────────────────────────────
# 可选模型: deepseek-ai/DeepSeek-R1-Distill-Qwen-1.5B / DeepSeek-R1-Distill-Qwen-7B / DeepSeek-R1-Distill-Llama-8B
DEEPSEEK_MODEL = "deepseek-ai/DeepSeek-R1-Distill-Qwen-1.5B"

print(f"[2/3] 加载 {DEEPSEEK_MODEL}...")
ds_tokenizer = AutoTokenizer.from_pretrained(DEEPSEEK_MODEL, trust_remote_code=True)
ds_model = AutoModelForCausalLM.from_pretrained(
    DEEPSEEK_MODEL,
    torch_dtype=torch.float16 if torch.cuda.is_available() else torch.float32,
    device_map="auto",
    trust_remote_code=True,
)
ds_model.eval()
print("DeepSeek 加载完成")

# ── 3. 翻译函数 ────────────────────────────────────────────
TRANSLATION_PROMPT = """你是一个专业的中文翻译。请将以下英文图像描述翻译成自然、地道的中文。只输出翻译结果，不要加任何解释。

英文: {text}
中文:"""

def translate_with_deepseek(text: str) -> str:
    """使用 DeepSeek 进行高质量中英翻译"""
    messages = [
        {"role": "system", "content": "你是一个专业的中文翻译，只输出翻译结果。"},
        {"role": "user", "content": TRANSLATION_PROMPT.format(text=text)},
    ]
    prompt_text = ds_tokenizer.apply_chat_template(
        messages, tokenize=False, add_generation_prompt=True
    )
    inputs = ds_tokenizer(prompt_text, return_tensors="pt").to(ds_model.device)
    with torch.no_grad():
        outputs = ds_model.generate(
            **inputs,
            max_new_tokens=128,
            temperature=0.1,  # 低温度保证翻译稳定
            do_sample=False,
        )
    # 只提取生成的部分（去掉输入）
    result = ds_tokenizer.decode(
        outputs[0][len(inputs.input_ids[0]):],
        skip_special_tokens=True
    ).strip()
    return result

def generate_caption_en(image):
    """BLIP 生成英文描述"""
    inputs = blip_processor(image, return_tensors="pt")
    with torch.no_grad():
        output = blip.generate(**inputs, max_new_tokens=50)
    return blip_processor.decode(output[0], skip_special_tokens=True)

# ── 4. 对比翻译：MarianMT vs DeepSeek ─────────────────────
from transformers import MarianMTModel, MarianTokenizer
marian_tokenizer = MarianTokenizer.from_pretrained("Helsinki-NLP/opus-mt-en-zh")
marian = MarianMTModel.from_pretrained("Helsinki-NLP/opus-mt-en-zh")

img_dir = "images"
exts = {".jpg", ".jpeg", ".png", ".bmp"}
img_files = sorted([f for f in os.listdir(img_dir) if os.path.splitext(f)[1].lower() in exts])

os.makedirs("results", exist_ok=True)
with open("results/translation_compare.txt", "w", encoding="utf-8") as f:
    for fname in img_files:
        image = Image.open(os.path.join(img_dir, fname)).convert("RGB")
        caption_en = generate_caption_en(image)

        # MarianMT 翻译
        inputs_mt = marian_tokenizer(caption_en, return_tensors="pt", padding=True)
        with torch.no_grad():
            zh_mt = marian_tokenizer.decode(
                marian.generate(**inputs_mt)[0], skip_special_tokens=True
            )

        # DeepSeek 翻译
        zh_ds = translate_with_deepseek(caption_en)

        print(f"[{fname}]")
        print(f"  EN:        {caption_en}")
        print(f"  MarianMT:  {zh_mt}")
        print(f"  DeepSeek:  {zh_ds}\n")

        f.write(f"=== {fname} ===\n")
        f.write(f"EN:        {caption_en}\n")
        f.write(f"MarianMT:  {zh_mt}\n")
        f.write(f"DeepSeek:  {zh_ds}\n\n")

print("翻译对比结果已保存到 results/translation_compare.txt")
```

---

### 5.3 YOLO + BLIP 区域描述：`caption_yolo_region.py`

**优化目标**：先用 YOLO 检测图中物体 → 裁剪每个物体的 bounding box → 用 BLIP 对每个区域单独描述 → 汇总为结构化输出。

```python
"""
YOLO 目标检测 + BLIP 区域描述
用法: python caption_yolo_region.py
"""
import torch, os
from PIL import Image, ImageDraw, ImageFont
from transformers import BlipProcessor, BlipForConditionalGeneration
from ultralytics import YOLO

# ── 1. 加载模型 ────────────────────────────────────────────
print("加载 YOLO...")
yolo = YOLO("yolo11n.pt")  # 自动下载，约 6MB；也可用 yolov8n.pt

print("加载 BLIP...")
blip_processor = BlipProcessor.from_pretrained("Salesforce/blip-image-captioning-base")
blip = BlipForConditionalGeneration.from_pretrained("Salesforce/blip-image-captioning-base")
blip.eval()

# ── 2. 从原图裁剪区域 ──────────────────────────────────────
def crop_region(image, box):
    """根据 [x1, y1, x2, y2] 裁剪区域"""
    x1, y1, x2, y2 = map(int, box)
    return image.crop((max(0, x1), max(0, y1), min(image.width, x2), min(image.height, y2)))

def describe_region(region_image):
    """对裁剪区域生成描述"""
    inputs = blip_processor(region_image, return_tensors="pt")
    with torch.no_grad():
        output = blip.generate(**inputs, max_new_tokens=30)
    return blip_processor.decode(output[0], skip_special_tokens=True)

# ── 3. 处理单张图片 ────────────────────────────────────────
def analyze_image(img_path, conf_threshold=0.3):
    """
    对图片做完整分析: 整体描述 + 各区域描述
    返回: {整体描述, [(标签, 置信度, bbox, 区域描述), ...]}
    """
    image = Image.open(img_path).convert("RGB")

    # 3a. 整体描述
    global_caption = describe_region(image)

    # 3b. YOLO 检测
    results = yolo(image, conf=conf_threshold)
    detections = []
    if len(results[0].boxes) > 0:
        boxes = results[0].boxes.xyxy.cpu().numpy()
        classes = results[0].boxes.cls.cpu().numpy()
        confs = results[0].boxes.conf.cpu().numpy()
        names = results[0].names  # {class_id: class_name}

        for box, cls_id, conf in zip(boxes, classes, confs):
            label = names[int(cls_id)]
            region = crop_region(image, box)
            if region.width > 10 and region.height > 10:  # 过滤过小区域
                region_desc = describe_region(region)
                detections.append({
                    "label": label, "confidence": float(conf),
                    "bbox": [int(v) for v in box],
                    "description": region_desc,
                })

    return {"global_caption": global_caption, "detections": detections}

# ── 4. 可视化 ──────────────────────────────────────────────
def draw_results(image, result, save_path):
    """绘制检测框 + 标注"""
    draw = ImageDraw.Draw(image)
    # 尝试加载中文字体，失败则用默认
    try:
        font = ImageFont.truetype("C:/Windows/Fonts/simhei.ttf", 16)
        font_small = ImageFont.truetype("C:/Windows/Fonts/simhei.ttf", 12)
    except:
        font = font_small = ImageFont.load_default()

    colors = ["red", "blue", "green", "orange", "purple", "cyan", "magenta", "yellow"]
    for i, det in enumerate(result["detections"]):
        x1, y1, x2, y2 = det["bbox"]
        color = colors[i % len(colors)]
        draw.rectangle([x1, y1, x2, y2], outline=color, width=3)
        draw.text((x1, y1 - 20), f"{det['label']}", fill=color, font=font)
        draw.text((x1, y2 + 4), det["description"][:40], fill=color, font=font_small)

    image.save(save_path)
    print(f"可视化结果: {save_path}")

# ── 5. 批量运行 ────────────────────────────────────────────
img_dir = "images"
exts = {".jpg", ".jpeg", ".png", ".bmp"}
img_files = sorted([f for f in os.listdir(img_dir) if os.path.splitext(f)[1].lower() in exts])

os.makedirs("results/yolo_regions", exist_ok=True)

with open("results/yolo_region_results.txt", "w", encoding="utf-8") as f:
    for fname in img_files:
        img_path = os.path.join(img_dir, fname)
        print(f"\n处理: {fname}")

        result = analyze_image(img_path)
        f.write(f"=== {fname} ===\n")
        f.write(f"整体描述: {result['global_caption']}\n")
        f.write(f"检测到 {len(result['detections'])} 个物体:\n")
        for i, det in enumerate(result["detections"]):
            f.write(f"  [{i+1}] {det['label']} (置信度: {det['confidence']:.2f})\n")
            f.write(f"      区域描述: {det['description']}\n")
            f.write(f"      位置: {det['bbox']}\n")
        f.write("\n")

        # 可视化
        image = Image.open(img_path).convert("RGB")
        draw_results(image, result, f"results/yolo_regions/{fname}")

print("\n所有结果已保存到 results/yolo_region_results.txt")
print("可视化图片保存在 results/yolo_regions/")
```

---

### 5.4 BLIP 图文匹配检索：`caption_retrieval.py`

**优化目标**：利用 BLIP 的 ITM（Image-Text Matching）能力，从多个候选描述中选出与图片最匹配的描述。

**原理**：BLIP 的 `BlipForImageTextRetrieval` 模型可计算图文匹配分数，本质是 `BlipForConditionalGeneration` 的扩展架构。由于 `blip-image-captioning-base` 不含 ITM 头，我们改用 `blip-itm-base-coco`，或用 captioning 模型 + 余弦相似度做近似匹配。

```python
"""
图文匹配检索: 从候选描述中选最优匹配
方案A: 使用 BLIP ITM 模型直出匹配分数
方案B: 用 CLIP/BLIP 双塔做 embedding 余弦相似度（兼容性更好）
用法: python caption_retrieval.py
"""
import torch, os
import torch.nn.functional as F
from PIL import Image
from transformers import (
    BlipProcessor,
    BlipForConditionalGeneration,
    BlipForImageTextRetrieval,  # BLIP 的 ITM 模型
)

# ── 1. 加载模型 ────────────────────────────────────────────
print("加载 BLIP Captioning...")
cap_processor = BlipProcessor.from_pretrained("Salesforce/blip-image-captioning-base")
cap_model = BlipForConditionalGeneration.from_pretrained("Salesforce/blip-image-captioning-base")
cap_model.eval()

print("加载 BLIP ITM (图文匹配)...")
itm_model = BlipForImageTextRetrieval.from_pretrained("Salesforce/blip-itm-base-coco")
itm_processor = BlipProcessor.from_pretrained("Salesforce/blip-itm-base-coco")
itm_model.eval()

# ── 2. 生成候选描述 ────────────────────────────────────────
def generate_candidates(image, n=5):
    """用 Beam Search 生成 n 个候选描述"""
    inputs = cap_processor(image, return_tensors="pt")
    with torch.no_grad():
        outputs = cap_model.generate(
            **inputs, max_new_tokens=50,
            num_beams=n * 2, num_return_sequences=n,
            output_scores=True, return_dict_in_generate=True,
        )
    captions = []
    for seq in outputs.sequences:
        captions.append(cap_processor.decode(seq, skip_special_tokens=True))
    return captions

# ── 3. ITM 打分 ────────────────────────────────────────────
def score_image_text_match(image, captions):
    """
    对每个候选描述计算图文匹配分数
    返回: [(caption, itm_score), ...]  sorted by score desc
    """
    scores = []
    for cap in captions:
        inputs = itm_processor(image, cap, return_tensors="pt")
        with torch.no_grad():
            outputs = itm_model(**inputs)
            # itm_score: 匹配(1) vs 不匹配(0) 的 logit
            itm_score = torch.softmax(outputs.itm_score, dim=1)[0, 1].item()
        scores.append((cap, itm_score))
    scores.sort(key=lambda x: x[1], reverse=True)
    return scores

# ── 4. 批量处理 ────────────────────────────────────────────
img_dir = "images"
exts = {".jpg", ".jpeg", ".png", ".bmp"}
img_files = sorted([f for f in os.listdir(img_dir) if os.path.splitext(f)[1].lower() in exts])

os.makedirs("results", exist_ok=True)
with open("results/itm_results.txt", "w", encoding="utf-8") as f:
    for fname in img_files:
        image = Image.open(os.path.join(img_dir, fname)).convert("RGB")

        # 生成候选
        candidates = generate_candidates(image, n=5)
        # ITM 排序
        ranked = score_image_text_match(image, candidates)

        f.write(f"=== {fname} ===\n")
        for i, (cap, score) in enumerate(ranked):
            marker = " ← 最优" if i == 0 else ""
            f.write(f"  [{i+1}] ITM={score:.4f}: {cap}{marker}\n")
        f.write("\n")

        print(f"{fname}: best → {ranked[0][0]} (ITM={ranked[0][1]:.4f})")

print("\nITM 匹配结果已保存到 results/itm_results.txt")
```

---

### 5.5 GPU 批量加速推理：`caption_batch_gpu.py`

**优化目标**：利用 GPU 批量推理能力，将多张图片打包成一个 batch 同时送入 BLIP，大幅提升吞吐量。

**核心技巧**：
- 统一图片尺寸后 stack 成 batch tensor
- 利用 `accelerate` 自动管理 device placement
- 对比单张 vs 批量速度

```python
"""
GPU 批量加速推理: DataLoader + batch inference
用法: python caption_batch_gpu.py
"""
import torch, os, time
from torch.utils.data import Dataset, DataLoader
from PIL import Image
from transformers import BlipProcessor, BlipForConditionalGeneration
from transformers import MarianMTModel, MarianTokenizer

# ── 1. 自定义 Dataset ─────────────────────────────────────
class ImageDataset(Dataset):
    def __init__(self, img_dir, exts=None):
        if exts is None:
            exts = {".jpg", ".jpeg", ".png", ".bmp"}
        self.files = sorted([
            os.path.join(img_dir, f) for f in os.listdir(img_dir)
            if os.path.splitext(f)[1].lower() in exts
        ])

    def __len__(self):
        return len(self.files)

    def __getitem__(self, idx):
        path = self.files[idx]
        image = Image.open(path).convert("RGB")
        return image, os.path.basename(path)

# ── 2. 批量推理函数 ────────────────────────────────────────
def collate_fn(batch):
    """将多张图片处理为 batch tensor"""
    images, names = zip(*batch)
    # BLIP processor 原生支持 batch
    inputs = processor(
        images=list(images),
        return_tensors="pt",
        padding=True,
    )
    return inputs, names

def batch_generate_captions(inputs):
    """一次推理整个 batch"""
    # 移到 GPU
    if torch.cuda.is_available():
        inputs = {k: v.cuda() for k, v in inputs.items()}
    with torch.no_grad():
        outputs = model.generate(
            **inputs,
            max_new_tokens=50,
            num_beams=3,  # batch 内每张用 beam=3 兼顾质量速度
        )
    # 解码（processor.batch_decode）
    captions = processor.batch_decode(outputs, skip_special_tokens=True)
    return captions

# ── 3. 主流程 ──────────────────────────────────────────────
device = "cuda" if torch.cuda.is_available() else "cpu"
print(f"使用设备: {device}")

print("加载模型...")
processor = BlipProcessor.from_pretrained("Salesforce/blip-image-captioning-base")
model = BlipForConditionalGeneration.from_pretrained("Salesforce/blip-image-captioning-base")
model.to(device)
model.eval()

# 翻译模型
marian_tok = MarianTokenizer.from_pretrained("Helsinki-NLP/opus-mt-en-zh")
marian = MarianMTModel.from_pretrained("Helsinki-NLP/opus-mt-en-zh")

# ── 4. 对比: 逐张 vs 批量 ─────────────────────────────────
dataset = ImageDataset("images")
print(f"共 {len(dataset)} 张图片\n")

batch_size = min(4, len(dataset))  # 根据显存调整
dataloader = DataLoader(dataset, batch_size=batch_size, collate_fn=collate_fn)

# 逐张基准测试
print("=== 逐张推理（基准）===")
t0 = time.time()
for image, name in dataset:
    inputs = processor(image, return_tensors="pt").to(device)
    with torch.no_grad():
        out = model.generate(**inputs, max_new_tokens=50)
    cap = processor.decode(out[0], skip_special_tokens=True)
    print(f"  {name}: {cap}")
t_single = time.time() - t0
print(f"逐张耗时: {t_single:.1f}s\n")

# 批量推理
print(f"=== 批量推理 (batch_size={batch_size}) ===")
t0 = time.time()
all_results = []
for inputs, names in dataloader:
    captions = batch_generate_captions(inputs)
    for name, cap in zip(names, captions):
        # 翻译
        zh_in = marian_tok(cap, return_tensors="pt", padding=True)
        with torch.no_grad():
            zh_out = marian.generate(**zh_in)
        zh = marian_tok.decode(zh_out[0], skip_special_tokens=True)
        all_results.append((name, cap, zh))
        print(f"  {name}: {cap} → {zh}")
t_batch = time.time() - t0

# ── 5. 速度对比 ─────────────────────────────────────────────
os.makedirs("results", exist_ok=True)
with open("results/batch_gpu_results.txt", "w", encoding="utf-8") as f:
    f.write(f"设备: {device}\n")
    f.write(f"图片数量: {len(dataset)}\n")
    f.write(f"Batch size: {batch_size}\n")
    f.write(f"逐张耗时: {t_single:.1f}s\n")
    f.write(f"批量耗时: {t_batch:.1f}s\n")
    f.write(f"加速比: {t_single / t_batch:.1f}x\n")
    f.write("-" * 40 + "\n")
    for name, en, zh in all_results:
        f.write(f"{name}\n  EN: {en}\n  ZH: {zh}\n\n")

print(f"逐张: {t_single:.1f}s | 批量: {t_batch:.1f}s | 加速: {t_single / t_batch:.1f}x")
print("结果已保存到 results/batch_gpu_results.txt")
```

---

## 六、整合运行脚本：`run_all.py`

一键顺序执行所有功能模块：

```python
"""
一键运行所有实验模块
用法: python run_all.py
"""
import subprocess
import sys

scripts = [
    ("基础单图推理", "caption.py"),
    ("基础批量推理", "batch_caption.py"),
    ("Beam Search 优化", "caption_beam.py"),
    ("DeepSeek 翻译对比", "caption_translate_llm.py"),
    ("YOLO + BLIP 区域描述", "caption_yolo_region.py"),
    ("图文匹配检索", "caption_retrieval.py"),
    ("GPU 批量加速", "caption_batch_gpu.py"),
]

for name, script in scripts:
    print(f"\n{'='*60}")
    print(f"  {name}: {script}")
    print(f"{'='*60}")
    try:
        subprocess.run([sys.executable, script], check=True)
    except subprocess.CalledProcessError as e:
        print(f"  [WARNING] {script} 运行出错: {e}")
    except FileNotFoundError:
        print(f"  [SKIP] {script} 文件不存在，跳过")

print("\n所有模块执行完毕，结果保存在 results/ 目录")
```

---

## 七、验证清单

完成实验后，逐项检查以下内容：

- [ ] **基础功能**：`caption.py` 能生成英文描述 + 中文翻译 + 可视化图片
- [ ] **批量推理**：`batch_caption.py` 遍历 images/ 目录，输出 `results/results.txt`
- [ ] **Beam Search**：`caption_beam.py` 输出 top-3 候选及分数，择优后翻译质量提升
- [ ] **大模型翻译**：`caption_translate_llm.py` 输出 MarianMT vs DeepSeek 对比，DeepSeek 翻译明显更自然
- [ ] **YOLO 区域**：`caption_yolo_region.py` 正确检测物体并标注，区域描述合理
- [ ] **图文匹配**：`caption_retrieval.py` 为每张图选出 ITM 分数最高的描述
- [ ] **GPU 加速**：`caption_batch_gpu.py` 输出加速比（通常 2-5x）
- [ ] **所有结果** 均写入 `results/` 目录下对应文件

---

## 八、常见问题

| 问题 | 解决方案 |
|------|----------|
| `CUDA out of memory` | 减小 `batch_size`，换更小的 DeepSeek 模型（1.5B），或用 CPU 推理 |
| DeepSeek 下载慢 | 设置 `export HF_ENDPOINT=https://hf-mirror.com` 使用镜像 |
| 中文字体显示方块 | 安装 SimHei 字体，或修改 `font_path` 指向系统已有中文字体 |
| YOLO 检测不到物体 | 降低 `conf_threshold`（默认 0.3 → 0.15） |
| BLIP ITM 模型加载失败 | 确认网络可访问 HuggingFace，或预先下载到本地目录 |
